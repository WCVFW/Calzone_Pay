Server (Express.js) for Recharge App.

1. Copy .env.example to .env and fill DB + JWT + Razorpay keys.
2. Install:
   cd server
   npm install
3. Create DB:
   Use server/schema.sql to create database & tables.
4. Start server:
   npm run dev   (if nodemon installed) or npm start
